<!DOCTYPE html>
<html>
<head>
  <title>MyappJobnews</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Job</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <% if user_signed_in? %>
            <% if current_user && current_user.admin? %>
            <li>
              <a class="nav-link <%= 'active' if current_page?(new_user_registration_path) %>" href="<%= new_user_registration_path %>">Registrar Usuario</a>
            </li>
            <% end %>
            <li>
              <%= button_to "Cerrar Sesion", destroy_user_session_path, method: :delete, class: "nav-link btn btn-link" %>
            </li>
          <% else %>
            <li>
              <a class="nav-link <%= 'active' if current_page?(new_user_session_path) %>" href="<%= new_user_session_path %>" <%= 'aria-current="page"' if current_page?(new_user_session_path) %>>Login</a>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container top-bar">
    <%= yield %>
  </div>
</body>
</html>
