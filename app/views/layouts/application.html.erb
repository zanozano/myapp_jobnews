<!DOCTYPE html>
<html>
  <head>
    <title>MyappJobnews</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Job</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link btn btn-link" %>
              </li>
              <li class="nav-item <%= 'active' if current_page?(new_user_registration_path) %>">
                <a class="nav-link <%= 'active' if current_page?(new_user_registration_path) %>" href="<%= new_user_registration_path %>" <%= 'aria-current="page"' if current_page?(new_user_registration_path) %>>Sign up</a>
              </li>
            <% else %>
              <li class="nav-item <%= 'active' if current_page?(new_user_session_path) %>">
                <a class="nav-link <%= 'active' if current_page?(new_user_session_path) %>" href="<%= new_user_session_path %>" <%= 'aria-current="page"' if current_page?(new_user_session_path) %>>Login</a>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
